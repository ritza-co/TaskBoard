version: '3.8'

services:
  taskboard:
    build: ./taskboard
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=${DATABASE_URL:-file:./prisma/dev.db}
    volumes:
      - ./taskboard/prisma/dev.db:/app/prisma/dev.db